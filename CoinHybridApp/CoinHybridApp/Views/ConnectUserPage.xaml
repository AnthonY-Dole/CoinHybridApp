<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="CoinHybridApp.Views.ConnectUserPage">
    <ContentPage.Content>
        <StackLayout Margin="10,10,10,10">
            <StackLayout VerticalOptions="StartAndExpand">
                <Label Text="Adresse mail"/>
                <Entry Text="{Binding Mail, Mode=TwoWay}"/>
            </StackLayout>
            <StackLayout VerticalOptions="StartAndExpand">
                <Label Text="Mot de passe"/>
                <Entry Text="{Binding Password, Mode=TwoWay}" IsPassword="True"/>
            </StackLayout>
            <StackLayout VerticalOptions="EndAndExpand">
                <Button Text="Valider" Margin="20,0,20,20" HeightRequest="50" CornerRadius="10" VerticalOptions="End" BackgroundColor="#3498db" TextColor="White" Clicked="connectToUser" />
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>